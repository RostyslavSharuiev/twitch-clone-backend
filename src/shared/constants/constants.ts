export const MIN_PASSWORD_LENGTH = 8;

export const MAX_BIO_LENGTH = 300;

export const TOTP_PIN_LENGTH = 6;

export const FILE_SUPPORTED_FORMAT_LIST = ['jpg', 'jpeg', 'png', 'webp', 'gif'];

export const MAX_FILE_SIZE_LIMIT = 10 * 1024 * 1024;

export const DEFAULT_STREAM_TAKE_NUMBER = 12;

export const DEFAULT_STREAM_SKIP_NUMBER = 0;
